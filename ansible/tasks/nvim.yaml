# neovim itself is installed from Brewfile.cli.
- name: Setup NvChad Neovim Configuration
  ignore_errors: true
  git:
    repo: "https://github.com/NvChad/NvChad"
    dest: "~/.config/nvim"
    depth: 1
  tags:
    - install
    - productivity
    - cli
    - nvim
